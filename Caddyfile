cafedailycravings.in, www.cafedailycravings.in {
    encode zstd gzip

    # backend API + Stripe webhook
    handle /api/* {
        reverse_proxy backend:8001
    }

    # frontend (React SPA)
    handle {
        reverse_proxy frontend:80
    }
}
